# =============================================================================
# Example secrets file for local/dev environment
# -----------------------------------------------------------------------------
# WARNING:
# - Do NOT commit real secrets into the repository.
# - This file is only an example/template for local and dev usage.
# - In production all real secrets must be stored in an external Vault/KMS
#   according to the security requirements of the project.
# =============================================================================

# ----------------------------------------------------------------------
# Bybit API credentials (testnet or main, depending on your environment)
# ----------------------------------------------------------------------
# Эти переменные подставляются в config/settings.yaml через плейсхолдеры
# ${BYBIT_API_KEY} и ${BYBIT_API_SECRET} (см. BybitConfigSchema).
BYBIT_API_KEY=your_key_here
BYBIT_API_SECRET=your_secret_here

# ----------------------------------------------------------------------
# Database & Redis connection (optional for local/dev)
# ----------------------------------------------------------------------
# Если заданы, перекрывают значения из config/settings.yaml.
# Пример DSN для локального PostgreSQL:
# DATABASE_URL=postgresql://avi5:avi5@localhost:5432/avi5
#
# Пример URL для локального Redis:
# REDIS_URL=redis://localhost:6379/0
#
# Для совместимости со скриптами и тестами могут использоваться также:
# DB_DSN=postgresql://avi5:avi5@localhost:5432/avi5
# REDIS_DSN=redis://localhost:6379/0

# DATABASE_URL=
# REDIS_URL=
# DB_DSN=
# REDIS_DSN=

# ----------------------------------------------------------------------
# Backup / DR
# ----------------------------------------------------------------------
# S3-бакет, в который scripts/backup.py и scripts/restore.py
# заливают и из которого берут backup’ы БД.
# BACKUP_S3_BUCKET=my-avi5-backups-bucket
# BACKUP_S3_BUCKET=

# ----------------------------------------------------------------------
# JWT secrets
# ----------------------------------------------------------------------
# Secret key for signing JWT tokens in dev environment.
# In production JWT_SECRET must be managed by KMS/Vault and never live in .env.
JWT_SECRET=your_jwt_secret
